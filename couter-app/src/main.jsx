import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)

// it's just a file if you look at it call a function
//ReactDOM.createRoot(document.getElementById('root)).rennder(
// <App/> this application your first component
//)